# 本地打印机转网络打印机需求文档

## 1. 背景与目标

本系统只解决一个问题：把当前 Windows 主机上已经可用的本地打印机转换成局域网可访问的网络打印机。

系统不开发打印机驱动，不做云打印，不做账号体系，不做复杂打印内容处理。打印机必须已经能在服务端主机上正常打印，服务只负责接收网络打印数据并转发到目标打印机队列。

核心目标：

- 使用 Web 界面进行配置和状态查看。
- 服务端暴露一个控制端口，用于 Web 界面和控制接口。
- 服务端暴露一个打印机数据端口，用于接收客户端打印数据。
- 未配置目标打印机时，默认转换系统默认打印机。
- 用户可以在 Web 界面中指定要转换的打印机。
- 服务启动后长期运行，将目标打印机表现为网络打印入口。

## 2. 使用场景

### 2.1 默认打印机自动转换

用户安装并启动服务后，如果没有进行任何打印机配置，系统自动读取 Windows 当前默认打印机，并将其发布为网络打印机。

适用场景：

- 用户只需要快速把当前默认打印机共享到局域网。
- 主机上只有一台常用打印机。
- 用户不希望先做复杂配置。

### 2.2 指定打印机转换

用户打开 Web 控制界面，在打印机列表中选择一台已安装打印机。保存后，服务将该打印机作为网络打印目标。

适用场景：

- 主机上安装了多台打印机。
- 系统默认打印机不是希望共享的打印机。
- 需要在后续更换被转换的打印机。

### 2.3 局域网客户端打印

客户端通过服务端主机 IP 和打印机数据端口提交打印任务。服务接收数据后，把任务转交给当前目标打印机。

## 3. 范围定义

### 3.1 本期范围

- 提供 Web 控制界面。
- 服务端提供控制端口。
- 服务端提供打印机数据端口。
- 枚举 Windows 当前已安装打印机。
- 自动识别 Windows 系统默认打印机。
- 无配置时默认使用系统默认打印机。
- 允许用户指定目标打印机。
- 保存目标打印机和端口配置。
- 接收网络打印数据。
- 将打印数据转发到目标 Windows 打印机队列。
- 展示当前目标打印机、服务状态、控制地址、数据地址、最近任务和错误信息。
- 记录运行日志和打印任务日志。

### 3.2 非本期范围

- 不开发打印机硬件驱动。
- 不实现云打印、跨公网打印或账号体系。
- 不实现多用户、多租户或复杂权限管理。
- 不实现打印内容编辑、排版、预览。
- 不实现同时发布多台打印机。
- 不实现打印任务管理系统，只保留必要的最近任务状态。
- 不承诺客户端完全免驱，客户端提交的数据必须能被目标打印机或 Windows 打印链路处理。

## 4. 用户与角色

- 管理员：安装和运行服务，打开 Web 界面，查看状态，指定打印机，调整端口。
- 客户端用户：通过网络打印地址提交打印任务。
- 后台服务：监听控制端口和数据端口，接收任务，转发到目标打印机，记录日志。

## 5. 总体方案

### 5.1 推荐架构：Web 控制端 + 后台服务

系统由一个后台服务提供全部能力：

- 控制端口：提供 Web 控制界面和本机控制 API。
- 打印机数据端口：接收客户端提交的打印数据。
- 打印转发模块：把接收到的数据提交到 Windows 打印队列。
- 配置模块：保存用户指定打印机、端口等配置。
- 日志模块：记录服务状态、任务状态和异常。

Web 界面由服务端直接提供，用户通过浏览器访问控制端口进行操作，不再开发独立桌面界面。

### 5.2 端口职责

#### 控制端口

控制端口用于：

- 打开 Web 控制界面。
- 获取服务运行状态。
- 枚举本机打印机列表。
- 查看当前目标打印机。
- 设置目标打印机。
- 设置或查看打印机数据端口。
- 查看最近任务和错误信息。

控制端口默认只绑定本机地址 `127.0.0.1`。如需要局域网访问 Web 控制界面，应由用户显式修改绑定地址，并承担内网访问风险。

#### 打印机数据端口

打印机数据端口用于：

- 监听局域网客户端连接。
- 接收打印任务原始数据。
- 将每次连接转换为一个打印任务。
- 把任务提交到当前目标打印机。

打印机数据端口默认绑定可被局域网访问的地址，默认协议优先采用 RAW TCP，默认端口建议为 `9100`。实际端口必须可配置，避免与已有服务冲突。

## 6. 功能需求

### 6.1 Web 控制界面

Web 界面应提供：

- 当前服务状态。
- 当前控制端口地址。
- 当前打印机数据端口地址。
- 当前目标打印机。
- 系统默认打印机。
- 已安装打印机列表。
- 指定目标打印机操作。
- 恢复使用系统默认打印机操作。
- 最近打印任务列表。
- 最近错误信息。
- 日志位置或日志查看入口。

Web 界面不承担复杂管理功能，只服务于“选择哪台打印机被转换为网络打印机”和“确认服务是否正常工作”。

### 6.2 打印机选择规则

系统应按以下优先级确定目标打印机：

1. 如果用户已配置指定打印机，使用用户指定打印机。
2. 如果用户未配置指定打印机，使用 Windows 当前系统默认打印机。
3. 如果指定打印机不存在且系统默认打印机存在，界面提示指定打印机失效，服务可回退到系统默认打印机。
4. 如果没有可用打印机，服务不接收打印任务，并在 Web 界面和日志中明确提示。

用户应可以在 Web 界面中：

- 查看所有已安装打印机。
- 查看哪一台是系统默认打印机。
- 选择某一台打印机作为固定目标。
- 清除固定目标，恢复默认使用系统默认打印机。

### 6.3 打印机数据接收

服务应在打印机数据端口接收客户端连接。每次完整连接或一次完整数据流应生成一条打印任务记录。

任务记录至少包含：

- 任务 ID。
- 来源 IP。
- 接收时间。
- 目标打印机。
- 数据大小。
- 当前状态。
- 错误信息。

服务应支持流式接收和转发，避免大文件一次性全部加载到内存。

### 6.4 打印任务转发

服务应将接收到的打印数据提交到 Windows 打印子系统或目标打印机队列。

状态说明：

- `received`：数据已接收。
- `submitted`：任务已提交到 Windows 打印队列。
- `failed`：任务接收或提交失败。

提交成功不等于物理打印完成。系统只要求确认任务已成功提交到 Windows 打印队列，不承诺追踪纸张实际输出完成。

### 6.5 服务运行

服务应支持：

- 启动。
- 停止。
- 重启。
- 查询状态。
- 加载配置。
- 保存配置。
- 长期后台运行。

服务启动时应自动执行目标打印机解析：

1. 加载配置。
2. 判断是否有用户指定打印机。
3. 如果没有指定打印机，读取系统默认打印机。
4. 启动控制端口。
5. 启动打印机数据端口。

### 6.6 配置持久化

配置至少包含：

- 控制端口绑定地址。
- 控制端口号。
- 打印机数据端口绑定地址。
- 打印机数据端口号。
- 用户指定目标打印机名称或稳定标识。
- 是否使用系统默认打印机。
- 日志级别。

如果配置文件不存在，系统使用默认配置启动，并自动选择系统默认打印机作为目标。

### 6.7 日志与诊断

日志应记录：

- 服务启动和停止。
- 控制端口监听状态。
- 打印机数据端口监听状态。
- 当前目标打印机解析结果。
- 用户修改目标打印机。
- 每个打印任务的接收、提交和失败状态。
- 端口占用、打印机不存在、权限不足、打印提交失败等异常。

日志不应默认保存打印内容正文，避免泄露文档内容。

## 7. 非功能需求

### 7.1 平台兼容

- 服务端目标平台为 Windows 10、Windows 11 和常见 Windows Server 版本。
- 目标打印机必须已经安装在服务端 Windows 主机上。
- 客户端需能向打印机数据端口发送目标打印机可处理的打印数据。

### 7.2 稳定性

- 服务应支持长期运行。
- 单个打印任务失败不应导致服务退出。
- 打印机离线、端口占用、客户端中断等异常应被隔离处理。
- 服务重启后应自动恢复控制端口、打印机数据端口和目标打印机解析。

### 7.3 安全性

- 控制端口默认只监听本机地址。
- 打印机数据端口默认只用于可信局域网。
- Web 界面应提示用户不要将端口暴露到公网。
- 日志不得保存打印正文。
- 如控制端口允许局域网访问，应至少提供明确风险提示；认证能力可作为后续扩展，不列入首期核心目标。

### 7.4 性能

- 普通办公文档应在接收完成后尽快提交到打印队列。
- 服务应限制单任务最大数据量，避免异常客户端耗尽资源。
- 大任务应采用流式处理。
- 并发任务应排队提交，避免同一目标打印机队列混乱。

### 7.5 可维护性

系统模块边界应保持简单：

- Web 控制模块。
- 配置模块。
- 打印机发现模块。
- 数据端口监听模块。
- 打印转发模块。
- 日志模块。

所有模块都围绕“把一台本地打印机转换为网络打印机”这一目标设计，不引入无关扩展。

## 8. 系统边界与数据流

### 8.1 组件边界

- Web 控制模块：提供浏览器界面和控制接口。
- 配置模块：读取、写入、校验端口和目标打印机配置。
- 打印机发现模块：枚举本机打印机并识别系统默认打印机。
- 数据端口监听模块：接收客户端打印数据。
- 打印转发模块：提交任务到 Windows 打印队列。
- 日志模块：记录运行日志和任务状态。

### 8.2 默认启动数据流

1. 用户启动服务。
2. 服务加载配置。
3. 配置中没有指定打印机。
4. 服务读取 Windows 系统默认打印机。
5. 服务启动控制端口。
6. 服务启动打印机数据端口。
7. 客户端向数据端口提交打印数据。
8. 服务将数据提交到系统默认打印机。
9. Web 界面展示任务状态和当前目标打印机。

### 8.3 指定打印机数据流

1. 用户打开 Web 控制界面。
2. Web 界面请求打印机列表。
3. 用户选择目标打印机。
4. 服务保存配置。
5. 服务切换当前目标打印机。
6. 后续打印任务转发到用户指定打印机。

## 9. 权限要求

- 访问部分打印机队列可能需要服务运行账户具备打印权限。
- 修改服务安装方式或 Windows 服务启动类型可能需要管理员权限。
- 如果目标打印机依赖用户会话凭据，需确认后台服务账户是否可以正常提交打印任务。
- 控制端口绑定到局域网地址或打印机数据端口开放给局域网时，可能需要配置 Windows 防火墙规则。

## 10. 异常处理要求

- 无配置且无系统默认打印机：服务启动控制端口，但数据端口不接收任务，Web 界面提示需要设置默认打印机或指定打印机。
- 指定打印机不存在：提示指定打印机失效；如果系统默认打印机可用，可回退到默认打印机并记录 warning。
- 打印机离线：任务标记为 `failed`，记录失败原因。
- 控制端口占用：服务启动失败，日志记录被占用端口，用户修改控制端口后再启动。
- 打印机数据端口占用：数据端口启动失败，Web 界面提示修改端口。
- 客户端连接中断：当前任务标记为 `failed`，不影响其他任务。
- 打印提交失败：任务标记为 `failed`，记录 Windows 打印接口返回的错误。
- 权限不足：Web 界面和日志提示调整服务账户或以管理员权限处理。

## 11. 验收标准

- 启动服务后，可以通过浏览器访问 Web 控制界面。
- 服务端存在一个控制端口和一个打印机数据端口。
- 未配置目标打印机时，系统自动使用 Windows 默认打印机作为目标。
- Web 界面可以展示当前系统默认打印机。
- Web 界面可以展示已安装打印机列表。
- 用户可以指定某台打印机作为网络打印目标。
- 用户可以清除指定打印机并恢复使用系统默认打印机。
- 局域网客户端可以向打印机数据端口提交打印数据。
- 打印任务可以被提交到当前目标打印机队列。
- 打印机不存在、端口占用、权限不足、提交失败等异常有明确提示和日志。

## 12. 测试需求

### 12.1 单元测试

- 默认配置加载。
- 配置文件读写。
- 系统默认打印机解析。
- 指定打印机解析。
- 打印机不存在时的回退策略。
- 端口配置校验。
- 任务状态流转。

### 12.2 集成测试

- Web 控制端口启动和访问。
- 打印机数据端口启动和连接。
- 无配置时自动选择系统默认打印机。
- 指定打印机后任务转发到指定打印机。
- 清除指定打印机后任务转发到系统默认打印机。
- 打印任务提交到 Windows 打印队列。

### 12.3 手工验收测试

- 只有默认打印机时的快速启动测试。
- 多打印机环境下指定打印机测试。
- 修改 Windows 默认打印机后的行为测试。
- 指定打印机被删除后的回退测试。
- 控制端口占用测试。
- 打印机数据端口占用测试。
- Windows 防火墙拦截测试。
- 真实打印机打印测试。

## 13. 风险与约束

- RAW TCP 打印要求客户端发送的数据能被目标打印链路处理。
- 不同打印机驱动对后台服务账户的兼容性可能不同。
- Windows 防火墙可能阻止局域网客户端访问打印机数据端口。
- 控制端口如果开放到局域网，需要额外考虑访问控制风险。
- 提交到 Windows 打印队列成功不代表物理打印最终成功。
- 如果客户端仍需要特定驱动，本系统不能完全消除驱动依赖。

## 14. 建议里程碑

### 14.1 最小可用版本

- 启动 Web 控制端口。
- 启动打印机数据端口。
- 读取系统默认打印机。
- 接收一条打印数据并提交到默认打印机。
- 显示当前状态和最近任务。

### 14.2 指定打印机版本

- 枚举打印机列表。
- 在 Web 界面指定目标打印机。
- 保存配置。
- 清除指定打印机并恢复默认打印机。
- 处理指定打印机不存在的回退逻辑。

### 14.3 稳定性版本

- 完善日志。
- 完善端口占用提示。
- 完善权限不足提示。
- 增加大文件流式处理。
- 增加异常任务隔离。

## 15. 待确认事项

- 控制端口默认端口号是否固定，例如 `8080`。
- 打印机数据端口默认端口号是否固定为 `9100`。
- 控制端口是否只允许本机访问，还是允许局域网访问。
- 数据端口首期是否只支持 RAW TCP。
- 是否需要安装程序自动配置 Windows 防火墙规则。
